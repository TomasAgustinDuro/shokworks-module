# Speciality Carousel Module

## 📋 Description

React responsive module that displays a carousel of specialty cards. Includes horizontal scrolling functionality, API consumption, and adaptive design for different devices.

## ✨ Features

- **Horizontal carousel** with smooth scroll
- **Responsive design** for mobile, tablet and desktop
- **API consumption** for dynamic data
- **Loading and error states**
- **Button navigation**
- **Infinite scroll** (loop)

## 🚀 Installation

### Prerequisites
- Node.js (version 14 or higher)
- yarn

### Steps to run locally

1. **Clone the repository**
```bash
git clone [repository-url]
cd [project-name]
```

2. **Install dependencies**
```yarn install ```
3. **Run in development mode**
```yarn start ```
4. **Open in browser**
```http://localhost:3000 ```

### Project estructure
```src/
├── components/
│   ├── Container/
│   │   ├── Container.jsx
│   │   └── container.module.css
│   └── Card/
│       ├── CardTemp.jsx
│       └── card-style.module.css
├── services/
│   └── service.js
└── App.js```
